# =============================================================================
# 사내망 개발 — 프로젝트 루트에 `.env.local` 로 복사한 뒤 값만 채움
# 옵션 B: 사내 PostgreSQL + MinIO — 계획: docs/사내망_이전_계획_baa87487.plan.md
# 통합 S3/MinIO 키 전체: env.example.txt (사내망 B 섹션)
# ---------------------------------------------------------------------------
# ※ `deploy/rocky/.env` = Docker Compose(서버) 전용. Next/Prisma 는 읽지 않음.
# =============================================================================

# ----- 연결 방식 -----
# A) DB/MinIO 를 내부 IP:포트로 직접
# B) SSH 터널: ssh -N -L 15432:127.0.0.1:5432 -L 19000:127.0.0.1:9000 -p 6022 design@<서버>
#    → 아래 127.0.0.1:15432, S3 127.0.0.1:19000

# ===== Database (사내 PostgreSQL) =====
DATABASE_URL="postgresql://USER:PASSWORD@127.0.0.1:15432/design5?schema=public&connection_limit=1"
DIRECT_URL="postgresql://USER:PASSWORD@127.0.0.1:15432/design5?schema=public"

# ===== NextAuth / 앱 URL =====
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="openssl rand -base64 32 로 생성"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ===== Google OAuth (사내 NEXTAUTH_URL 에 맞춰 콘솔 URI 등록) =====
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ===== 사내망 MinIO (S3 호환) — env.example.txt S3_ 변수와 동일 키 =====
# deploy/rocky/.env 의 MINIO_ROOT_USER / MINIO_ROOT_PASSWORD 를 여기 S3 키에 맞출 수 있음(개발용).
S3_ENDPOINT="http://127.0.0.1:19000"
S3_REGION="us-east-1"
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""
S3_FORCE_PATH_STYLE="true"
# (선택) Nginx/게이트웨이 뒤 공개 URL이 정해지면. 미정이면 Presigned/서버 경유만
S3_PUBLIC_BASE_URL=""

S3_BUCKET_POSTS="posts"
S3_BUCKET_EDMS="edms"
S3_BUCKET_AVATARS="avatars"
S3_BUCKET_ICONS="icons"
S3_BUCKET_PPT_THUMBNAILS="ppt-thumbnails"

# (선택) DB·이미지 URL에 **구** 클라우드 도메인이 남아 있을 때 — env.example.txt legacy-asset-bases
