# ===== Database (Supabase PostgreSQL) =====
# Prisma용 PostgreSQL 연결 URL. Supabase 사용 시 connection_limit=1 권장 (Session 모드 연결 풀)
# 예: postgresql://user:password@host:5432/db?schema=public&connection_limit=1
DATABASE_URL="postgresql://user:password@localhost:5432/layerary?schema=public"

# 마이그레이션 실행용 직접 연결 URL (Transaction 모드). Supabase의 DIRECT_URL 사용
DIRECT_URL="postgresql://user:password@localhost:5432/layerary?schema=public"

# ===== NextAuth =====
# 애플리케이션 베이스 URL (로컬: http://localhost:3000, 배포: https://your-domain.com)
NEXTAUTH_URL="http://localhost:3000"

# NextAuth 세션 암호화용 시크릿. openssl rand -base64 32 로 생성 권장
NEXTAUTH_SECRET="your-secret-key-here"

# ===== Google OAuth (선택) =====
# Google OAuth 로그인 사용 시 필요. 미설정 시 이메일/비밀번호 로그인만 가능
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ===== Backblaze B2 =====
# 이미지/파일 저장용 B2 인증 정보 (다이어그램, 게시물 썸네일, PPT ZIP 등)
B2_APPLICATION_KEY_ID=""
B2_APPLICATION_KEY=""
B2_BUCKET_ID=""
B2_BUCKET_NAME=""
B2_ENDPOINT="https://s3.us-west-004.backblazeb2.com"
# B2 버킷을 private으로 두고 Cloudflare Worker로 공개할 경우, Worker 공개 URL (예: https://assets.layerary.com). 설정 시 업로드 후 반환되는 fileUrl이 이 주소를 사용합니다.
B2_PUBLIC_URL="https://assets.layerary.com"
# (선택) 클라이언트에서 B2 이미지 프록시 여부 판별용. B2_PUBLIC_URL과 동일하게 두면 Worker URL은 프록시 없이 직접 로드됩니다.
NEXT_PUBLIC_B2_PUBLIC_URL="https://assets.layerary.com"
# (선택) B2 CORS 추가 출처. npm run b2:setup-cors 실행 시 허용할 출처를 쉼표로 구분 (예: https://preview.layerary.com). layerary.com / www는 스크립트에 기본 포함.
# B2_CORS_ORIGINS="https://preview.layerary.com"

# ===== Cloudflare R2 (eDM 이미지) =====
# eDM 이미지 저장용 R2. R2_PUBLIC_URL 설정 시 공개 URL 방식(만료 없음), 미설정 시 Presigned URL(7일)
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME="edms"
# 공개 액세스용 기준 URL (예: https://cdn.layerary.com). 설정 시 이메일 HTML 이미지가 만료 없이 동작
R2_PUBLIC_URL="https://cdn.layerary.com"

# ===== Supabase (DB, Keepalive 등) =====
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
# 서버 측 Supabase API 호출용 (Keepalive 등)
SUPABASE_SERVICE_ROLE_KEY=""

# ===== Keepalive (선택) =====
# GitHub Actions에서 /api/keepalive 호출 시 보안용. 설정 시 Vercel + GitHub Secrets에 동일 값 등록
# KEEPALIVE_SECRET=""

# ===== 기타 =====
# 클라이언트에서 참조하는 앱 URL (예: CORS, 리다이렉트)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
